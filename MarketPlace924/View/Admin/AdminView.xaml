<Page
    x:Class="MarketPlace924.View.AdminView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketPlace924.View.Admin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:domain="using:MarketPlace924.Domain"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<ListView x:Name="UsersListView" Height="300" Grid.Row="0" ItemsSource="{Binding Users}">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="domain:User">
					<local:UserRowView />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<!-- Pie Chart Placeholder -->
		<Grid Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,20,0,0">
			<Ellipse Fill="LightGray" Width="200" Height="200"/>
			<TextBlock Text="Pie Chart Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center" />
		</Grid>
	</Grid>
</Page>
