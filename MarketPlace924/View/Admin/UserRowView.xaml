<UserControl
    x:Class="MarketPlace924.View.Admin.UserRowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketPlace924.View.Admin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid Padding="10">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="150"/>
			<ColumnDefinition Width="150"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

		<TextBlock Text="{Binding UserRowViewModel.Username}" VerticalAlignment="Center"/>
		<TextBlock Text="{Binding UserRowViewModel.Role}" Grid.Column="1" VerticalAlignment="Center"/>
		<Button Content="Ban" Click="{Binding UserRowViewModel.BanUserCommand}" Grid.Column="2" VerticalAlignment="Center"/>
	</Grid>
</UserControl>
